{
  "name": "project-monitor-platform",
  "version": "1.0.0",
  "description": "Integrated Project Monitoring and Academic Planner Platform",
  "main": "server/src/server.js",
  "scripts": {
    "build:client": "npm install --prefix client && npm run build --prefix client",
    "build:server": "npm install --prefix server",
    "build": "npm run build:server && npm run build:client",
    "start": "node server/src/server.js",
    "dev": "npm run build:client && node --watch server/src/server.js",
    "test": "npm test --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
